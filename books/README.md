# books/

책별 작업 디렉토리 루트다.

권장 규칙:

- 폴더명은 slug 사용: 예) `the-big-print`
- 각 책은 최소한 아래 하위 디렉토리를 가진다.
  - `source/` : 원문 추출물, 메타데이터, 목차
  - `content/` : 정리된 요약/챕터 Markdown 또는 JSON
  - `quiz/` : 퀴즈 데이터
  - `site/` : 렌더된 HTML 결과물

새 책을 추가할 때는 이 구조를 복제해서 사용한다.
