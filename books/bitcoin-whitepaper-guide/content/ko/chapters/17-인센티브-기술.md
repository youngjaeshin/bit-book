# 6. 인센티브 — 기술

비트코인 백서 6장의 기술적 이해는 코인베이스 거래의 실제 데이터 구조를 살펴보고, 비트코인에 정말 사전 채굴이 없었는지를 코드 수준에서 직접 검증하는 과정이다.

## 코인베이스 거래의 실제 데이터 구조

블록 안에는 여러 거래가 담기며 각 거래에는 번호가 붙는다. 블록에서 가장 첫 번째인 0번째 거래는 항상 코인베이스 거래이다. 코인베이스 거래는 이전 거래를 참조하지 않으므로, 입력값(vin)에는 최대 100바이트의 임의 데이터를 넣을 수 있다. 출력값(vout)에는 채굴자의 지갑 주소가 적힌다.

제네시스 블록의 코인베이스 거래는 비트코인 코어 소스코드에 하드코딩되어 있다. txid는 `4a5e1e4baab89f3a32518a88c31bc87f618f76673e2cc77ab2127b7afdeda33b`이며, 입력의 coinbase 필드에는 사토시 나카모토가 숨겨놓은 메시지가 16진수로 인코딩되어 있다. 이를 디코딩하면 영국 신문 타임스 2009년 1월 3일자 헤드라인인 "The Times 03/Jan/2009 Chancellor on brink of second bailout for banks"가 나온다. 이는 기존 금융 체계에 대한 조소이자, 제네시스 블록의 생성 시각을 증명하는 타임스탬프이다. 출력값은 50 BTC이며, 사토시 나카모토의 공개키로 잠겨 있다(P2PK 형식).

높이 854,689 블록은 코인베이스 입력에 "Made in USA"가 최초로 새겨진 블록이다. 이처럼 채굴자 또는 채굴 풀 운영 기업은 코인베이스 입력에 원하는 메시지를 새길 수 있다.

## 신뢰하지 말고, 검증하라: 비트코인에 정말 사전 채굴이 없었을까

비트코인은 오픈 소스이므로 코드 전체가 공개되어 있다. 제네시스 블록은 다른 노드로부터 데이터를 받아오는 것이 아니라 비트코인 코어 자체에 하드코딩되어 있다. 이 내용은 `src/kernel/chainparams.cpp` 파일의 41–78번째 줄에서 확인할 수 있다.

63–65번째 줄의 주석은 다음과 같이 명시하고 있다.

> Build the genesis block. Note that the output of its generation transaction cannot be spent since it did not originally exist in the database.
>
> (제네시스 블록을 생성합니다. 생성된 트랜잭션의 출력은 데이터베이스에 존재하지 않기 때문에 사용할 수 없습니다.)

풀 노드들은 블록 내의 거래를 검증하며 아직 소비되지 않은 출력들을 UTXO 세트에 저장한다. 그런데 `src/validation.cpp` 파일의 2481–2487번째 줄에는 다음 주석과 함께 제네시스 블록 처리를 건너뛰는 코드가 있다.

> Special case for the genesis block, skipping connection of its transactions (its coinbase is unspendable)
>
> (제네시스 블록에 대한 특수한 사례로, 해당 블록의 트랜잭션 연결을 생략합니다. 해당 블록의 코인베이스 보상은 사용할 수 없습니다.)

제네시스 블록에 한해서만 거래를 데이터베이스에 추가하지 않고 건너뛴다. 따라서 누군가 제네시스 블록의 코인베이스 보상을 사용하려 해도 UTXO 세트에 존재하지 않으므로, 풀 노드들은 이를 유효하지 않은 거래로 판단하고 폐기한다. 해당 거래는 네트워크에 퍼질 수 없다.

실제로 풀 노드를 운영한다면 `bitcoin-cli getrawtransaction` 명령어로 제네시스 블록 코인베이스 보상의 txid를 조회해볼 수 있다. 조회 결과는 다음과 같다.

```json
{
  "result": null,
  "error": {
    "code": -5,
    "message": "The genesis block coinbase is not considered an ordinary transaction and cannot be retrieved"
  },
  "id": null
}
```

제네시스 블록의 코인베이스는 일반 트랜잭션으로 간주되지 않으며 조회할 수 없다는 메시지가 반환된다. 이로써 비트코인이 코드 수준에서 철저하게 사전 채굴을 차단했음이 검증된다. 사토시 나카모토는 비트코인을 세상에 공개하기 전에 자신의 몫을 따로 챙기지 않았다.

## 세그윗 이전의 수수료 단위와 이후의 변화

세그윗 도입 이전에는 수수료 단위가 SATS/B(사토시/바이트)였다. 세그윗 도입 이후에는 가상 바이트(virtual bytes, vB)라는 단위를 사용하며, 수수료는 SATS/vB로 표시된다. 수수료가 3 SATS/vB이고 거래 데이터의 가상 크기가 100 vB라면 수수료는 300 SATS이다. 현재 적정 수수료는 mempool.space에서 실시간으로 확인할 수 있다.

거래소에서 개인 지갑으로 비트코인을 옮길 때 수수료가 지나치게 많이 나온다면, 그것은 비트코인 프로토콜의 수수료 때문이 아니라 거래소가 자체적으로 부과하는 수수료 때문이다.

## 요약

코인베이스 거래는 비트코인 공급의 유일한 원천이며, 그 구조는 실제 블록 데이터에서 직접 확인할 수 있다. 제네시스 블록의 코인베이스 보상은 비트코인 코어 코드에 의해 영구적으로 소비 불가능하게 처리되어 있으며, 이는 사전 채굴이 없었음을 코드 수준에서 증명한다. 비트코인의 철학은 "신뢰하지 말고, 검증하라"는 원칙 위에 서 있다.
