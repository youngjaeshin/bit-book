# 3. 타임스탬프 서버 — 기본

## Chapter Digest

비트코인 작동 원리를 이해하기 위한 첫 번째 열쇠는 해시 함수다. 해시 함수는 네 가지 핵심 특징을 갖는다. 첫째는 일가성이다. 같은 입력값을 넣으면 항상 같은 출력값이 나온다. 둘째는 고정된 길이 출력이다. 입력 데이터가 두 글자든 건물 전체의 데이터든 출력값의 길이는 항상 같다. 비트코인에서 주로 쓰이는 SHA256 함수는 어떤 데이터를 넣어도 항상 16진수 64자리, 즉 256비트의 출력값을 낸다. 셋째는 눈사태 효과다. 입력값이 단 한 글자만 달라져도 출력값은 완전히 다른 값이 된다. 넷째는 단방향성이다. 출력값을 보고 입력값을 역산하는 것은 불가능하다. 이 단방향성 덕분에 해시 함수는 보안 분야에서 광범위하게 쓰인다. 비밀번호를 평문으로 저장하는 대신 해시값을 저장하는 방식이 대표적이다. 해싱하여 나온 출력값은 입력 데이터를 압축한 대푯값, 즉 디지털 지문의 역할을 한다.

블록은 검증된 거래 데이터를 모아 담는 단위다. 거래 데이터가 블록에 들어가려면 먼저 검증을 거쳐야 한다. 검증자는 입력값에 있는 이전 거래 참조 번호로 해당 거래를 찾아 실재 여부를 확인하고, 출력의 공개키로 서명의 유효성을 검증한다. 문제없는 거래만 블록에 담긴다. 블록은 거래 데이터와 함께 블록의 속성을 기록한 블록 헤더를 갖는다. 블록 헤더에는 블록 내 모든 거래 데이터를 해싱하여 만든 머클 루트, 대략적인 생성 시각인 타임스탬프, 난이도 관련 값인 논스와 비트값, 그리고 이전 블록의 해시값이 들어간다.

블록을 대표하는 식별값은 블록 헤더 전체를 해싱하여 만든 블록 해시값이다. 블록 헤더 안에 모든 거래 데이터의 요약값인 머클 루트가 포함되어 있으므로, 블록 해시값은 그 블록에 담긴 모든 거래 데이터를 압축한 대푯값과 같다. 핵심은 이 블록 헤더 안에 이전 블록의 해시값이 포함된다는 것이다. 2번째 블록의 헤더에 1번째 블록의 해시값이 포함된 채 해싱되면, 2번째 블록의 해시값은 1번째 블록의 모든 데이터를 품은 것과 같다. 3번째 블록은 2번째 블록의 해시값을 포함하여 해싱되므로, 이전 두 블록의 데이터도 모두 간접적으로 담게 된다. 이 연결이 계속 이어지면 블록들은 서로 뗄 수 없는 체인을 형성한다. 이것이 블록체인이다.

이 구조 덕분에 중간 블록의 데이터를 조금이라도 변조하면 해당 블록의 해시값이 바뀌고, 그 뒤에 이어지는 모든 블록의 해시값도 도미노처럼 바뀐다. 뒤에 많은 블록이 쌓일수록 중간 블록의 변조는 사실상 불가능해진다. 사토시 나카모토는 비트코인을 '거래를 위한 분산화된 안전한 타임스탬프 서버'라고 직접 표현했다. 블록 해시값이 곧 타임스탬프인 것이다. 같은 블록에 담긴 거래들은 동시에 일어난 것이고, 다음 블록에 담긴 거래들은 이전 블록의 거래들보다 나중에 일어난 것이다. 이렇게 블록 순서가 곧 시간의 흐름이 되어 거래의 선후관계가 확정된다. 이중지불 문제를 해결하는 핵심이 바로 이 선후관계의 확정이다. 분산화된 시스템에서 누구나 동의할 수 있는 거래 순서를 만들어내는 방법, 그것이 타임스탬프 서버의 본질이다.
