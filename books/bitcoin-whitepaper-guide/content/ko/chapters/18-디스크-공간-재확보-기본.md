# 7. 디스크 공간 재확보 — 기본

비트코인 백서 7장은 블록체인의 저장 공간 문제를 해결하는 방법인 머클 트리 구조를 설명하고, 분산화가 비트코인 설계의 핵심 가치임을 논한다.

## 머클 트리 구조: 해시값을 변경시키지 않고 거래 데이터 삭제하기

2024년 기준 풀 노드를 운영하려면 약 600GB 이상의 저장 공간이 필요하다. 어떤 거래의 출력값이 소비된 후 그 위로 충분한 블록이 쌓였다면, 해당 거래 데이터를 삭제해 저장 공간을 절약할 수 있지 않을까? 문제는 거래 데이터를 포함해 블록을 해싱한 경우, 거래 데이터를 제거하면 해시 함수의 눈사태 효과에 의해 블록의 해시값이 완전히 바뀐다는 점이다. 이를 해결하는 방법이 머클 트리 구조이다.

해시값은 입력 데이터를 압축해 대표하는 값이다. 어떤 거래 데이터를 해싱해 얻은 txid(거래 식별자)는 그 거래 데이터를 해싱하지 않고서는 얻을 수 없다. 따라서 블록을 해싱할 때 거래 데이터 전체를 포함하지 않고 각 거래의 txid만 포함해도, 거래 데이터 전체가 담겨있는 것과 같은 효과를 낸다.

머클 트리 구조는 이 원리를 한 단계 더 발전시킨 것이다. 블록 안의 거래 txid들을 두 개씩 짝지어 해싱하고, 그 결과로 나온 해시값들을 다시 두 개씩 짝지어 해싱하는 과정을 반복한다. 해시값 개수가 홀수일 때는 마지막 해시값을 복사해 짝수를 맞춘다. 이 과정을 반복해 최종적으로 단 하나의 32바이트 해시값이 남는데, 이를 머클 루트라고 한다. 뒤집힌 나무 모양과 같아서 '머클 트리', '이진 트리', '해시 트리'라고도 부른다.

블록에 4,000개의 거래가 있고 각 거래가 평균 250바이트라면, 전체 거래 데이터는 1MB이다. txid만 저장해도 4,000개 × 32바이트 = 128KB로 줄고, 머클 루트 하나만 저장하면 32바이트가 된다. 원래 거래 데이터의 약 30,000분의 1로 줄어드는 것이다.

이처럼 블록 헤더에는 머클 루트 하나만 포함하고 블록 헤더만 해싱하므로, 거래 데이터를 삭제해도 블록의 해시값은 변하지 않는다. 거래 데이터를 삭제하고 블록 헤더만 보관하는 노드를 SPV 노드 또는 라이트 노드라고 한다.

## 왜 하필 머클 트리 구조인가

모든 거래를 통째로 한 번에 해싱하고 그 값 하나만 블록에 포함시켜도 블록 해시값을 변경시키지 않을 수 있다. 그런데도 굳이 두 개씩 짝지어 반복 해싱하는 머클 트리 구조를 쓰는 이유는 두 가지이다.

첫째, 라이트 노드가 특정 거래가 블록에 포함되었는지 확인하기 위해 블록 전체 거래 데이터를 받아올 필요가 없다. 모든 거래를 통째로 해싱한 값이라면 라이트 노드가 해당 거래의 포함 여부를 확인하기 위해 블록 전체 거래 데이터를 다운로드해야 한다. 그러나 머클 트리 구조에서는 해당 거래의 경로에 있는 해시값들(머클 증명)만 받아오면 확인할 수 있다. 이것이 다음 장에서 설명하는 '간소화된 지불 검증(SPV)'의 기반이 된다.

둘째, 머클 트리는 위변조 탐지에 탁월하다. 특정 거래가 위변조되었을 때, 단순 해싱이라면 어떤 거래가 문제인지 하나하나 대조해야 한다. 그러나 머클 트리 구조에서는 어떤 쌍의 해시값이 달라졌는지 추적함으로써 위변조된 거래를 빠르게 찾아낼 수 있다.

## 저장 공간과 분산화

블록 헤더 크기는 약 80바이트이다. 블록이 10분마다 생성된다고 가정하면, 1년에 80바이트 × 6 × 24 × 365 = 약 4.2MB의 저장 공간이 필요하다. 스마트폰으로 저화질 사진 한 장을 찍어도 4MB인 점을 감안하면, 블록 헤더 저장은 아무 문제가 없다. 전체 거래 데이터를 포함하더라도 2024년 기준 약 10만 원짜리 외장 SSD로 해결된다.

그렇다면 사토시 나카모토는 왜 저장 공간에 이토록 신경을 썼는가? 바로 분산화 때문이다. 거래를 검증하는 노드가 많아질수록 비트코인 네트워크는 강건해진다. 그런데 노드 운영에 과도한 비용이 든다면 이는 참여의 장벽이 된다. 10만 원은 사정이 어려운 학생이나 낙후된 지역 사람들에게는 사치일 수 있다. 비트코인은 특정 누군가를 위한 화폐 시스템이 아니므로, 거래 검증에 참여하고자 하는 모든 사람이 참여할 수 있어야 한다.

비트코인은 분산화에 모든 것을 건 시스템이다. 속도나 기능이 더 좋더라도 분산화가 없다면 그 화폐는 모두를 위한 화폐가 아니다. 비트코인의 블록 용량이 제한되어 온체인 거래가 느리다는 것은 사실이다. 그러나 비트코인은 최종 정산 시스템이며, 일상적 소액 결제는 라이트닝 네트워크라는 제2레이어가 담당한다. 기반 레이어의 분산화와 불변성을 지키면서 그 위에 제2레이어가 확장성을 담당하는 구조가 비트코인의 설계 철학이다.

한때 일부 비트코이너들은 교환의 매개체 기능을 위해 분산화를 일부 희생해야 한다고 주장했다. 이 갈등은 '블록사이즈 전쟁'으로 불리는 커다란 논쟁을 낳았고, 그 결과와 의미에 대해서는 심층적 이해에서 자세히 살펴볼 것이다.
