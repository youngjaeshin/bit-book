# Blind Robbery segmentation rules

## Decision

- Use the English EPUB as source of truth.
- Convert the 18 TOC entries into 11 web-summary units.
- Merge the front matter (`Title` through `Introduction`) into one opening unit.
- Keep the 9 numbered chapters as primary web units.
- Merge `References` + `About the authors` into one closing unit.
